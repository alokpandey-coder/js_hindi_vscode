"use strict"; // treat all JS code as newer version.

//alert("hello") 

//datatypes

/*
   number  ==> 2 to power 53
   bigint
   string ==> ""
   boolean ==> true/false
   null ==> standalone value representation of empty value(it can be assigned as null like temp = null)
   undefined ==> not assigning value
   symbol ==> unique

   object
*/

console.log(typeof "ALOK");

console.log(typeof null);
console.log(typeof undefined);

